// src/components/Addresses.js
import React from 'react';

const SiteCurrentStock = () => {
  return (
    <div style={{textAlign: "center"}}>
      <h2>Current Stock</h2>
      {/* Addresses content goes here */}
    </div>
  );
};

export default SiteCurrentStock;
